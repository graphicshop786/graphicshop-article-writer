import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.graphicshoparticlewriter',
  appName: 'GraphicShopArticleWriter',
  webDir: 'dist'
};

export default config;
